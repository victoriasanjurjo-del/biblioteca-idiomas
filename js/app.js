/**
 * app.js — Punto de entrada principal de Language Library Web App
 */

import { UI } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
  UI.init();
  console.log('🌿 Language Library Web App iniciada correctamente.');
});
